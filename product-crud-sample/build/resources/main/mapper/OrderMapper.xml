<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.product.mapper.OrderMapper">

    <resultMap id="OrderMap" type="com.example.product.domain.Order">
        <id property="id" column="id"/>
        <result property="memberId" column="member_id"/>
        <result property="status" column="status"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.product.domain.Order"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders(member_id, status, total_amount)
        VALUES(#{memberId}, #{status}, #{totalAmount})
    </insert>

    <select id="findById" resultMap="OrderMap">
        SELECT id, member_id, status, total_amount, created_at, updated_at
        FROM orders
        WHERE id = #{id}
    </select>

    <update id="updateStatus">
        UPDATE orders
        SET status = #{status},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
